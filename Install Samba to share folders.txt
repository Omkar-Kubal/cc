Install Samba to share folders or files between Windows and Linux. (practical 10)

-------------------------------server side ---------------------------
sudo apt update -y
sudo apt install samba -y
sudo mkdir /share
sudo chmod 777 /share

sudo nano /etc/samba/smb.conf
 
after opening the file follow this 

Press Ctrl+end to get to the end of file.
Then we’ll write the shared definition

[my-samba-share]
 path = /share
 public = no
 valid users = tom, jerry
 read list = tom
 write list = jerry
 browseable = yes
 comment = "My Samba File Server"

Press ctrl+O to write to the file

sudo testparm

sudo useradd tom -s /sbin/nologin
sudo useradd jerry -s /sbin/nologin
sudo smbpasswd -a tom
(add password)
sudo smbpasswd -a jerry
(add password)

sudo systemctl start smbd
sudo systemctl start nmbd
sudo systemctl start smbd nmbd

check ip address of server machine 
ip a


--------------------------client side------------------------------------

open terminal and ping the server ip.
ping 192.168.61.128

Then open File Manager -> Other Locations -> connect to server ->
smb://192.168.61.128

click on connect, open file my samba share, connect as registered user, username tom, domain WORKGROUP, Password 123, remember password till you logout
connect file will open

------------------------------------------------------------------------
Windows Client Side: 
Ping the server to check whether we are in the network or not. 

ping 192.168.61.128


Press window + R then enter cmd. and type there \\192.168.61.128

open enter username password and enter 

